// 列表查询参数常量
export enum ListSearchConstant {
  LIST_SEARCH_PARAM_KEY = 'keyword',
  LIST_PAGE_PARAM_VALUE = 'page',
  LIST_PAGESIZE_PARAM_VALUE = 'pageSize',
  LIST_PAGE_SIZE = 10,
}

// 路由路径常量
export enum RouterPath {
  LOGON_PATHNAME = '/login',
  REGISTER_PATHNAME = '/regiest',
  HOME_PATHNAME = '/',
  MANAGE_INDEX_PATHNAME = '/manage/list',
}

// 本地存储key常量
export enum LocalstorageKey {
  LOGIN_USERNAME_KEY = 'USERNAME',
  LOGIN_PASSWORD_KEY = 'PASSWORD',
  LOGIN_TOKEN_KEY = 'TOEKN',
}
